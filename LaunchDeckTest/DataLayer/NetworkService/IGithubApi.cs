using System;
using Refit;

namespace LaunchDeckTest.DataLayer.NetworkService
{
    public interface IGithubApi
    {

        ////get current trip info
        //[Get("/users/{userName}")]
        //IObservable<Model.GetCurrentTrip.GetCurrentTripResponse>
                         //GetUserInfo([AliasAs("userName")]string userName);

    }
}
